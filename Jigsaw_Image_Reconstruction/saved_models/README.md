# Pre-trained Model

The trained model weights are not included in this repository because of their file size.

The notebook provides the procedure for downloading the pre-trained weights required for evaluation.
